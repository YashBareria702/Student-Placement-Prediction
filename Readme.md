# Placement Data Analytics Project

This project analyzes a dataset from Kaggle on student job placement using Python.

## Structure

- `data/`: Contains raw CSV dataset
- `outputs/`: Cleaned data & generated plots
- `notebooks/`: Jupyter notebooks for EDA and visualizations

## Setup

```bash
pip install -r requirements.txt
